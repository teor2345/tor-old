  o Minor enhancements (fallback directory mirrors):
    - Improve fallback consensus download checks. Improve logging.
      Restrict the consensus weight range in preparation for #17905.
      Add new fallback directory mirrors to the whitelist.
      Add fallback candidates with bad DirPort responses to the blacklist.
      Update fallback directories based on the latest OnionOO data.
      Closes task 17158 and bug 18689 on tor 0.2.8.1-alpha;
      patch by "teor".
